Nothing should be ignored based on the provided file changes. The added files are all JSON widget configuration files and a README markdown file, which are source/config files that should be tracked in version control. No build artifacts, dependencies, or temporary files are present in the changes.